{
    "coc.preferences.rootPatterns": [".git", ".root", "Cargo.toml", "compile_commands.json"],
	"languageserver": {
		"cmake": {
			"command": "cmake-language-server",
			"filetypes": ["cmake"],
			"rootPatterns": [".git", ".root", "compile_commands.json"],
			"initializationOptions": {
				"buildDirectory": "_build"
			}
		}
    },
    // change diagnostic sign
    "diagnostic.errorSign": "✖",
    "diagnostic.warningSign": "‼",

    // clangd arguments
    "clangd.arguments": ["--clang-tidy", "--completion-style=detailed"]
}
